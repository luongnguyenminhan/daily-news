// This UI card is shared by search results and saved links. The behavior remains
// feature-owned through its callbacks; the visual component is exposed globally.
export { ResultCard as ArticleCard } from "@/features/search/components/ResultCard";
